---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: obproxies.oceanbase.oceanbase.com
spec:
  group: oceanbase.oceanbase.com
  names:
    kind: OBProxy
    listKind: OBProxyList
    plural: obproxies
    shortNames:
    - obp
    singular: obproxy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .spec.obCluster.name
      name: Cluster
      type: string
    - jsonPath: .spec.proxyClusterName
      name: ProxyName
      type: string
    - jsonPath: .spec.withConfigServer
      name: CfgSrv
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OBProxy is the Schema for the obproxies API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OBProxySpec defines the desired state of OBProxy
            properties:
              image:
                description: Image is the OBProxy container image (e.g. oceanbase/obproxy-ce:tag).
                type: string
              obCluster:
                description: Reference to the target OBCluster.
                properties:
                  name:
                    description: Name of the OBCluster resource.
                    type: string
                  namespace:
                    description: Namespace of the referenced OBCluster. Defaults to
                      the namespace of the OBProxy resource.
                    type: string
                required:
                - name
                type: object
              parameters:
                description: Parameters are optional OBProxy configuration key-value
                  pairs (mapped to proxy startup config).
                items:
                  properties:
                    name:
                      type: string
                    value:
                      type: string
                  required:
                  - name
                  - value
                  type: object
                type: array
              proxyClusterName:
                description: ProxyClusterName is the OBProxy cluster (application)
                  name used inside OceanBase proxy.
                type: string
              proxySysSecret:
                description: |-
                  ProxySysSecret is the name of a Secret in the same namespace as this OBProxy that holds
                  the root@proxysys password. The Secret must contain a key named "password".
                type: string
              replicas:
                default: 1
                description: Replicas is the number of OBProxy pods.
                format: int32
                minimum: 1
                type: integer
              resource:
                description: Resource limits and requests for each OBProxy pod.
                properties:
                  cpu:
                    anyOf:
                    - type: integer
                    - type: string
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  memory:
                    anyOf:
                    - type: integer
                    - type: string
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                required:
                - memory
                type: object
              serviceType:
                default: ClusterIP
                description: ServiceType is the Kubernetes Service type exposed for
                  SQL access.
                enum:
                - ClusterIP
                - NodePort
                - LoadBalancer
                - ExternalName
                type: string
              withConfigServer:
                default: false
                description: |-
                  WithConfigServer declares whether OBProxy is started with a config server.
                  false (default) means RS_LIST bootstrap: the operator may derive RS_LIST from cluster topology and roll pods when it changes.
                  true means config-server mode: the operator must not treat RS_LIST as operator-maintained for rolling updates.
                type: boolean
            required:
            - image
            - obCluster
            - proxyClusterName
            - proxySysSecret
            - replicas
            - resource
            type: object
          status:
            description: OBProxyStatus defines the observed state of OBProxy
            properties:
              image:
                description: Image currently used by the OBProxy workload (observed).
                type: string
              operationContext:
                description: OperationContext carries long-running task progress when
                  using the operator task flow.
                properties:
                  failureRule:
                    properties:
                      failureStatus:
                        type: string
                      failureStrategy:
                        type: string
                      maxRetry:
                        type: integer
                      retryCount:
                        type: integer
                    required:
                    - failureStatus
                    - failureStrategy
                    type: object
                  idx:
                    type: integer
                  name:
                    type: string
                  targetStatus:
                    type: string
                  task:
                    type: string
                  taskId:
                    type: string
                  taskStatus:
                    type: string
                  tasks:
                    items:
                      type: string
                    type: array
                required:
                - idx
                - name
                - targetStatus
                - task
                - taskId
                - taskStatus
                - tasks
                type: object
              readyReplicas:
                description: ReadyReplicas is the number of ready OBProxy pods.
                format: int32
                type: integer
              replicas:
                description: Replicas is the desired replica count observed from the
                  workload.
                format: int32
                type: integer
              status:
                description: High-level phase, e.g. Running, Pending, Failed.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
