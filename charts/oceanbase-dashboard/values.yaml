initCredentials: true
# admin's password. If not set, the chart will generate it randomly
adminPassword: admin

userCredentials: 
userNamespace: 
configNamespace: 

service:
  type: NodePort
  port: 80
