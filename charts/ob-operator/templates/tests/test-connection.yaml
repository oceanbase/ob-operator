apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ob-operator.fullname" . }}-test-connection"
  labels:
    {{- include "ob-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ob-operator.fullname" . }}:8081']
  restartPolicy: Never
