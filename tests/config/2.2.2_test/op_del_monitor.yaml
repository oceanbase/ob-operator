apiVersion: "oceanbase.oceanbase.com/v1alpha1"
kind: OBClusterOperation
metadata:
  generateName: ${OP_DEL_MONITOR}
  namespace: ${NAMESPACE}
spec:
  obcluster: ${OBCLUSTER_NAME}
  type: ModifyOBServers
  modifyOBServers:
    removeMonitor: true

